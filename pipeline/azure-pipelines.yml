trigger:
  - master

schedules:
  - cron: '0 20 * * *'
    displayName: Daily Check
    branches:
      include:
        - master
    always: 'true'

variables: 
  majorMinorVersion: 1.0
  semanticVersion: $[counter(variables['majorMinorVersion'], 0)]
  versionNano: "1809"

resources:
  repositories:
    - repository: templates
      type: github
      name: Justin-DynamicD/azure_pipeline-templates
      endpoint: GitHub

jobs:
  - job: getJenkinsVer
    pool:
      vmImage: 'windows-2019'
    steps:
      - task: PowerShell@2
        name: checkJenkins
        displayName: Check available Jenkins version
        inputs:
          filepath: pipeline\checkNewBuilds.ps1
          arguments: -minorVer $(semanticVersion)
      - task: PowerShell@2
        name: generateBA
        displayName: Generate Build_Arguments
        inputs:
          targetType: inline
          script: write-output "##vso[task.setvariable variable=buildArg;isOutput=true]--build-arg VERSIONNANO=$(versionNano) --build-arg VERSIONJENKINS=$env:JENKINSVER"
        env:
          JENKINSVER: $[ dependencies.getJenkinsVer.outputs['checkJenkins.versionJenkins'] ]

  - template: docker_container.yml@templates
    parameters:
      Build_Arguments: $[ dependencies.getJenkinsVer.outputs['generateBA.buildArg'] ]
      Container_Name: winjenkins
      Container_Version: $[ dependencies.getJenkinsVer.outputs['checkJenkins.versionJenkins'] ]
      dependsOn: getJenkinsVer
      NewBuild: $[ dependencies.getJenkinsVer.outputs['checkJenkins.newBuild'] ]
      Pool: 'windows-2019'
